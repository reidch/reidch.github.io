## Result

The system uses a `64³` spatial grid and processes `2,097,152` particles at approximately `20–30 FPS` on the target machine.

## Engineering focus

- Spatial hashing and neighborhood search
- GPU memory layout
- Semi-implicit Euler integration
- Visual debugging of density and pressure