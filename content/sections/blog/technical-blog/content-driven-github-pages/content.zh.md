## 核心限制

浏览器无法安全地枚举静态服务器上的任意文件夹，因此这个仓库使用一个很小的构建脚本。

每次 Push 后，GitHub Actions 会：

1. 扫描所有栏目、分类与内容文件夹
2. 自动识别 `cover.*`、画廊图片、本地视频和代码文件
3. 读取三语 Markdown、视频链接和外部链接
4. 生成统一内容索引
5. 部署最终的 `_site` 文件夹

网页代码完全不需要知道有多少项目或作品。